# Azure Configuration (Optional - defaults shown)
# Override these by setting environment variables before running scripts

# Resource Group Configuration
AZURE_RESOURCE_GROUP=teamai-prod
AZURE_LOCATION=eastus

# Azure Container Registry
AZURE_ACR_NAME=teamairegistry

# Azure Key Vault
AZURE_KEYVAULT_NAME=teamai-vault

# PostgreSQL Configuration
AZURE_DB_SERVER=teamai-db
AZURE_DB_ADMIN_USER=adminuser
AZURE_DB_PASSWORD=SecureP@ssw0rd123!

# Redis Configuration
AZURE_REDIS_NAME=teamai-redis

# Container Apps Environment
AZURE_CONTAINER_ENV=teamai-env

# Google OAuth (Must be set in Key Vault manually)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret
